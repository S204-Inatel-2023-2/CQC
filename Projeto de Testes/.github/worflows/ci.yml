name: End-to-end tests

on: [push]
jobs:
  cypress-open:
    runs-on:
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Cypress run
        uses: cypress-io/github-actions@v5